services:
  zenoh-router:
    image: eclipse/zenoh:latest
    container_name: fms-zenoh-router
    network_mode: host
    command: ["--listen", "tcp/0.0.0.0:7447", "--rest-http-port", "8001"]
    restart: unless-stopped
